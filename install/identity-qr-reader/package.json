{
  "name": "@vialabs/identity-qr-reader",
  "version": "0.1.0",
  "private": true,
  "description": "Reference USB scanner → ViaAccess Identity dynamic QR redeem",
  "type": "module",
  "scripts": {
    "start": "tsx --env-file=.env src/scan-redeem.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.3",
    "typescript": "^5",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=20"
  }
}
