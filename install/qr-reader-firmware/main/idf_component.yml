dependencies:
  idf: ">=5.1"
  # LAN discovery: advertises <hostname>.local so /setup works without an IP.
  espressif/mdns: "^1.2.0"
  # Wear-levelled filesystem for config.json and the policy snapshot.
  joltwallet/littlefs: "^1.14.0"
  # DevKitC-1 onboard WS2812 status LED (RMT backend).
  espressif/led_strip: "^2.5.0"
